{
  "name": "poc-amiweb-patateur",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "build": "vite build",
    "dev": "vite --host 127.0.0.1",
    "preview": "vite preview --host 127.0.0.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "vite": "8.2.0"
  }
}
